# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Environment variables — NEVER commit these
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Uploads — imagens enviadas pelo admin existem SÓ no servidor de produção
# NUNCA devem ser sobrescritas pelo git deploy
public/uploads/*
!public/uploads/.gitkeep
dist/uploads/*

# Artefatos de deploy e dumps de banco — não versionar
deploy_hostinger.zip
hostinger_import.sql
